#include <stdio.h>

/**
 * main - entry point
 * @argc: count of cli arguments
 * @argv: command line arguments
 * Return: integer
*/
int main(int argc, char **argv)
{
if (argc > 0)
{
printf("%s\n", argv[0]);
}

return (0);
}
